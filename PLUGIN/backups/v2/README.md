# Backup v2 · Plan Viewer Plugin

Snapshot de la estructura de conformidad del plugin.

`files/plan-viewer-plugin/` apunta a los mismos objetos Git que la raíz instalable `PLUGIN/plan-viewer-plugin/` y `files/plans/` conserva el plan de diseño y las decisiones aprobadas.

Para restaurar, sustituye esas dos carpetas por las copias de `files/`. No modifica ni restaura la habilidad clásica `skill-backups/plan-viewer/`.
